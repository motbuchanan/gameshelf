KNIGHT SCHOOL — TOY BOX DEPLOYMENT (corrected)

Upload these THREE files to the gameshelf repo root, replacing existing:
  1. knightschool.html   (new — the trainer)
  2. index.html          (edited — Knight School added to the TOY BOX,
                          not the board-game shelf)
  3. sw.js               (edited — knightschool.html precached, cache v34 -> v35)

Knight School now appears as a TOY (with Harp Trainer, Piano Trainer,
The Orb, Dollhouse, Magic 8 Ball, BeatForge), not among the board games.

Test-phase, deliberately NOT done: no GAME_KEYS entry, no achievements
registration (the trainer runs its own inline shim; games + lesson
progress save on-device under ks_* keys). Merge path is inside
knightschool.html if it graduates to a permanent resident.

CAVEAT: this index.html was edited from a repo copy pulled earlier today.
If you've pushed index.html changes since, say so before uploading and
the toy-box insertion will be re-applied to the current file.
