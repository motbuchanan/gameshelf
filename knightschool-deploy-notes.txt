KNIGHT SCHOOL — TOY BOX DEPLOYMENT (all files pre-edited, plain upload)

Upload these THREE files to the gameshelf repo root, replacing the two
that exist:

  1. knightschool.html   (new — the trainer)
  2. index.html          (edited — Knight School card added beside Chess,
                          shelf count now 47)
  3. sw.js               (edited — knightschool.html precached, cache
                          bumped v34 -> v35 so devices re-seed offline copies)

That's the whole deployment. After upload, a plain refresh on Garrett's
tablet shows the card: "Knight School — Learn chess from square one."

Deliberately NOT done (test phase):
- No GAME_KEYS entry in gamenight.js — Knight School runs its own inline
  shim, so there's nothing for Firebase to sync. Adding it now would be a
  half-registration (the v34 silent-bug pattern).
- No achievements registration — same reason. Games and lesson progress
  save on-device under ks_* keys.

If Knight School graduates to permanent resident: the merge path is inside
knightschool.html (delete the shim block, restore the gamenight/sfx script
tags, then add ks_games to GAME_KEYS + achievements).
