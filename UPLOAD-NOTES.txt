KNIGHT SCHOOL — v36 (built on a FRESH pull of the live repo main)

Three files to the repo root:
  knightschool.html   the trainer (standalone, always safe)
  index.html          live index + Knight School in the TOY BOX, badge -> v36
  sw.js               live sw + knightschool precached, cache -> v36 (badge & sw now in sync)

READ THIS FIRST — one thing to confirm before uploading index.html / sw.js:
The live repo main, pulled fresh just now, is at v34/v35 and contains
NO Thumbstack and NO tagline system. If you shipped those from another
chat and expect them live, they are NOT on main — the real latest index/sw
live in that other chat, and you should upload THOSE (with knightschool added)
instead of these. If Thumbstack/taglines were never actually pushed to main,
then this v36 set is correct and safe to upload as-is.

knightschool.html is safe to upload no matter what — it is standalone and
version-independent.
